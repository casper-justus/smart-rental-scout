name: tenantmatch_app
description: TenantMatch – AI-powered rental matching app.
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.6
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^13.0.2
  cached_network_image: ^3.4.1
  flutter_map_cache: ^1.5.2
  dio: ^5.4.0
  dio_cache_interceptor_db_store: ^6.0.0
  panorama_viewer: ^2.0.7
  path_provider: ^2.1.1
  sensors_plus: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
